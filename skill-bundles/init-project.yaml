name: init-project
skills:
- hermes-sdd-orchestration
- openspec-sdd
description: 'Initialize the current repo for the SDD agency: openspec/ if missing, openspec/project.md,
  docs/architecture, docs/decisions, reports/ and the initialization report. Writes no code.'
instruction: 'STAGE: initialize-project. Read ~/.hermes/workflows/initialize-project.md in full before
  acting and run its preflight exactly as written. Resolve the project root (pwd -P + openspec context
  --json) and compare it against the expected root; if ambiguous, stop and report. Load the agents/personas
  the workflow names. Validate each agent''s output in the real repo before advancing. Close with templates/final-report.md.
  Never improvise stages or skip the gate.'

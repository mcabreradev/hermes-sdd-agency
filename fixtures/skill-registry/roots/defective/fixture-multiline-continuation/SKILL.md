---
name: fixture-multiline-continuation
description:
  This value continues on indented lines, which is legal YAML and must not be
  reported as a missing description.
---

# Fixture: multi-line continuation

The registry must flag this as MULTILINE-SCALAR (and never print a truncated value
or claim the description is missing).

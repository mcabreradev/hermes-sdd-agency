---
name: fixture-valid-tagged
description: A well-formed fixture skill that declares tags, used to exercise the registry's happy path.
metadata:
  hermes:
    tags: [fixture, registry, valid]
---

# Fixture: valid tagged skill

Body content that must never be mistaken for frontmatter.

description: this line is in the BODY, not the frontmatter

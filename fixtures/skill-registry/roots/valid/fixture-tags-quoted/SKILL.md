---
name: fixture-tags-quoted
description: A healthy skill whose tags are a quoted flow sequence, to check tag parsing.
metadata:
  hermes:
    tags: ["alpha", "beta"]
---

# Fixture: quoted flow-sequence tags

The registry must print the tag values without the surrounding quotes.
